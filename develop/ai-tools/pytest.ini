[pytest]
# Configure paths to search for tests
testpaths = tests

# Filter warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

# Configure test discovery
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Verbose output
addopts = -v
